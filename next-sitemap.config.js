/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://zaaptelas.com.br",
  generateRobotsTxt: true,
  changefreq: "monthly",
  priority: 1.0,
};

module.exports = config;
